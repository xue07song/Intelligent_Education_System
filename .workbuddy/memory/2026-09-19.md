# 2026-09-19

- 首次通读项目「智能题库管理系统 Intelligent_Education_System」，梳理前后端结构与核心功能，写入 MEMORY.md。
- 注意：根目录 Bash 工具在本机异常（`ls`/`dirname` not found），优先用 Glob/Read/Grep 等文件工具替代。
- PowerShell 工具本机 stdout 会被吞：长命令输出需 `*> log文件` 再 Read；含中文日志用 `Get-Content -Encoding UTF8 | Out-File -Encoding UTF8` 转存。

## 功能：面包屑可点击跳转（App.vue）
- 需求：教师/管理员顶栏面包屑「首页 / 出卷与学生管理 / 主观题复核」点击文字跳转对应页面。
- 实现（只改 `src/App.vue`）：
  - 新增 `breadcrumbSegments` computed：把 `meta.crumb` 按「 / 」拆段；最后一段（当前页）只展示，组段渲染为链接。
  - 组段落点 = 路由表顺序中「crumb 以该组名 + ' / ' 为前缀」且 `meta.roles` 含当前角色的第一条路由（教师→试卷列表；teacher-only 路由对管理员自动跳过）。
  - 跳转复用 `nav.js` 的 `goRoute`；新增 `.breadcrumb-link` 样式（灰→紫 hover）。
- `meta.crumb` 字符串未改，guard-matrix 341/341 通过，vite build 通过。

## 诊断：无法生成试卷
- 生成按钮禁用 = 组卷检查第 5 项「题型与难度组合」未过：examRuleEngine 用最大流做「题型分布 × 难度分布」对章节库存的交叉校验；单独看都够、交叉可能不够。
- 解决：等预览说明块出现后按提示/辅助组卷采用可行方案，或调难度分布/扩章节。

## cpolar 公网访问链接
- 用户提供了 cpolar authtoken，已写入 `C:\Users\Administrator\.cpolar\cpolar.yml`（免费版，账号武骏鹏）。
- cpolar 安装在 `D:\cpolar\cpolar.exe`，且本机装有 cpolar Windows 服务（预定义隧道：5173/8080/3389，服务日志在 `~\.cpolar\logs\`）。
- 为后端 3000 端口（托管 dist + API，同源，外网可用的正确入口）开隧道：`cpolar http 3000 -log=stdout -log-level=INFO` 后台运行，日志在 `.workbuddy/cpolar-tunnel2.log`。
- 公网链接：`https://363c2d3e.r39.cpolar.top`（12:51 应用户要求重新生成，旧链接 218e6cf2 已失效；health 200 验证通过），日志 `.workbuddy/cpolar-tunnel3.log`。免费版每次重启地址会变。
- 坑：cpolar CLI 直接跑 TUI 时 stdout 重定向为空（缓冲），必须加 `-log=stdout` 才能拿到地址；本地 9200 管理 API 返回 200 空body 不可用。5173 隧道对外无用（request.js baseURL 硬编码 localhost:3000）。
